import React from 'react';
import { StyleSheet, Text, View, TouchableOpacity } from 'react-native';

const Parksidemenu = (props) => {
    console.log(props.date);
    return (
        <View>
            <Text>Today's Parkside menu.</Text>
        </View>
    );
};

export default Parksidemenu;
