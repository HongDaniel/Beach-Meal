import React from 'react';
import { StyleSheet, Text, View, TouchableOpacity } from 'react-native';

const Hillsidemenu = (props) => {
    return (
        <View>
            <Text>This is Hillside menu.</Text>
        </View>
    );
};

export default Hillsidemenu;
